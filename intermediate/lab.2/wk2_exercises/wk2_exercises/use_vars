#!/bin/bash
# Name: Javier Hernandez Requena
# File: use_vars
# Desc: Creates variables and then uses them to accomplish the tasks assigned in Week 2 - TODO 7


docs=~/users/steve/documents
let=$docs/letters
prop=$docs/proposals


ls $docs

cp -rf $let/ $prop/

mv $let/*[A-Z]* .

ls -l $docs/memos | wc -l


